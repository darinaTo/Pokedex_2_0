package com.example.pokedex_2_0.network
/*

import com.example.pokedex_2_0.data.models.PokemonDatabase
import com.example.pokedex_2_0.data.models.request.Result


fun asDatabaseModel(list: List<Result> ): List<PokemonDatabase> {
    return list.map { entry ->
        PokemonDatabase(
            image = entry.url,
            name = entry.name
        )
    }
}
*/
